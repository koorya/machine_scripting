// var dialog = require('dialog');

setTimeout(()=>console.log('Ok'), 3000);

// // example, without setting title
// dialog.warn('is cam ok',  function(exitCode){
//     if (exitCode == 1) console.log('User closed window');
//     if (exitCode == 0) console.log('Ok');
// })
